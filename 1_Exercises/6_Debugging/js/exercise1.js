/* TThis function makes no sense, correct it
*/
function doSomething(number) {
    var numberOfSlides = 10;
    var slideWidth = 400;
    var baz;

    var foo = "The gallery has ;
    console.log(foo + numberofslides + ' photos');

    var naj = "naj ";
    for(var i = 0; i < numberOfSlides - 1; i++) {
      var bar = "Photo no " + i + " I like " + naj + " more";
      console.log(baz)
      naj += " naj";
    }
}
